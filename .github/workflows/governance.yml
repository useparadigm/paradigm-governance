name: Governance

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    name: Governance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
      - run: uv run governance-ast
